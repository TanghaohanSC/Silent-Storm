// silent-storm-port Phase 1.5 r6: empty - stay in intermission so user (or
// the screenshot_keys.ps1 driver) can open the console with backtick and
// type commands. Phase 1.5 r6 iter 1 confirmed `mainmenu` crashes inside
// CRenderBaseInterface::Initialize's NWorld::CreateWorld call.
