//============================================================================
// silent-storm-port Phase 1.5 r6: original autoexec.cfg pointed at
// c:\a5\cfg\* which exists only on Nival's dev box. Use relative paths so the
// game actually loads input.cfg's `bind` commands.
//============================================================================
exec .\cfg\input.cfg
exec .\cfg\config.cfg

//============================================================================
// User config
//mission_camera_limits 25 28.21 28.21 -0.854 -0.854
