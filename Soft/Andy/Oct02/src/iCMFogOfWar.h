#ifndef __INTERFACE_CHAPTERMAP_FOGOFWAR_H__
#define __INTERFACE_CHAPTERMAP_FOGOFWAR_H__
#if _MSC_VER > 1000
#pragma once
#endif // _MSC_VER > 1000
////////////////////////////////////////////////////////////////////////////////////////////////////
#include "DG.h"
#include "GSceneUtils.h"
#include "ChapterInfo.h"
////////////////////////////////////////////////////////////////////////////////////////////////////
namespace NGfx
{
	class CTexture;
	class CGeometry;
};
////////////////////////////////////////////////////////////////////////////////////////////////////
namespace NGScene
{
////////////////////////////////////////////////////////////////////////////////////////////////////
class ISW2DScene;
////////////////////////////////////////////////////////////////////////////////////////////////////
class CFogOfWarTexture: public CPtrFuncBase<NGfx::CTexture>
{
	OBJECT_BASIC_METHODS(CFogOfWarTexture);
private:
	ZDATA
	bool bUpdated;
	CArray2D<int> sFogMap;
	CPtr<ISW2DScene> pScene;
	ZEND int operator&( CStructureSaver &f ) { f.Add(2,&bUpdated); f.Add(3,&sFogMap); f.Add(4,&pScene); return 0; }
	
protected:
	void Recalc();
	bool NeedUpdate();

public:
	CFogOfWarTexture() {}
	CFogOfWarTexture( const SChapterInfo &sInfo );

	void AddSpot( NDb::CTexture *pTexture, const CVec2 &vPos, float fRadius );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
}
////////////////////////////////////////////////////////////////////////////////////////////////////
#endif