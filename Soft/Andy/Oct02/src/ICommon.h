#ifndef __COMMONDECORATORS_INTERFACE_H__
#define __COMMONDECORATORS_INTERFACE_H__
#if _MSC_VER > 1000
#pragma once
#endif // _MSC_VER > 1000
////////////////////////////////////////////////////////////////////////////////////////////////////
namespace NUI
{
////////////////////////////////////////////////////////////////////////////////////////////////////
const float
	F_BASE_TRANSPARENCY			= 0.7f,
	F_HILIGHT_TRANSPARENCY	= 1.0f;
const int
	N_UNITICON_FADE_TIME			= 500,
	N_UNITICON_INVENTORY_TIME	= 1000;
////////////////////////////////////////////////////////////////////////////////////////////////////
// CBackground
////////////////////////////////////////////////////////////////////////////////////////////////////
class CBackground: public CDecorator<IWindow>
{
	OBJECT_NOCOPY_METHODS(CBackground);
private:
	ZDATA_(TBaseClass)
	float fTransparency;
public:
	ZEND int operator&( CStructureSaver &f ) { f.Add(1,(TBaseClass*)this); f.Add(2,&fTransparency); return 0; }

public:
	CBackground() {}
	CBackground( IWindow *pContainer, float fTransparency );

	bool ProcessMessage( const SEvent &sEvent );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
} // Namespace
////////////////////////////////////////////////////////////////////////////////////////////////////
#endif
